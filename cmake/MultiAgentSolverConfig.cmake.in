
set(MultiAgentSolver_INCLUDE_DIRS "${_IMPORT_PREFIX}/include")

# Sanity check for the include directory existence
if(NOT EXISTS "${MultiAgentSolver_INCLUDE_DIRS}")
  message(FATAL_ERROR "File or directory referenced by variable MultiAgentSolver_INCLUDE_DIRS (${MultiAgentSolver_INCLUDE_DIRS}) does not exist !")
endif()

set(MultiAgentSolver_LIBRARIES MultiAgentSolver) # This should refer to the interface target name

include(CMakeFindDependencyMacro)
find_dependency(Eigen3 REQUIRED)
find_dependency(OsqpEigen REQUIRED)

find_dependency(OpenMP REQUIRED)

# Include the targets file generated by install(TARGETS ... EXPORT ...)
include("${CMAKE_CURRENT_LIST_DIR}/MultiAgentSolverTargets.cmake")

# Final check for targets (optional, but good practice for robust configs)
check_required_components(MultiAgentSolver)